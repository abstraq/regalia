import 'package:flutter/material.dart';

class AppShadows {
  static const darkElevation06 = BoxShadow(
    color: Color(0x52000000),
    offset: Offset(0, 20),
    blurRadius: 32,
    spreadRadius: 0,
  );

  static const darkElevation062 = BoxShadow(
    color: Color(0x14000000),
    offset: Offset(0, 2),
    blurRadius: 8,
    spreadRadius: 0,
  );

  static const darkElevation05 = BoxShadow(
    color: Color(0x52000000),
    offset: Offset(0, 16),
    blurRadius: 24,
    spreadRadius: 0,
  );

  static const darkElevation052 = BoxShadow(
    color: Color(0x0a000000),
    offset: Offset(0, 2),
    blurRadius: 8,
    spreadRadius: 0,
  );

  static const darkElevation04 = BoxShadow(
    color: Color(0x52000000),
    offset: Offset(0, 8),
    blurRadius: 16,
    spreadRadius: 0,
  );

  static const darkElevation042 = BoxShadow(
    color: Color(0x0a000000),
    offset: Offset(0, 2),
    blurRadius: 4,
    spreadRadius: 0,
  );

  static const darkElevation03 = BoxShadow(
    color: Color(0x52000000),
    offset: Offset(0, 4),
    blurRadius: 8,
    spreadRadius: 0,
  );

  static const darkElevation032 = BoxShadow(
    color: Color(0x0a000000),
    offset: Offset(0, 0),
    blurRadius: 2,
    spreadRadius: 0,
  );

  static const darkElevation02 = BoxShadow(
    color: Color(0x52000000),
    offset: Offset(0, 2),
    blurRadius: 4,
    spreadRadius: 0,
  );

  static const darkElevation022 = BoxShadow(
    color: Color(0x0a000000),
    offset: Offset(0, 0),
    blurRadius: 1,
    spreadRadius: 0,
  );

  static const darkElevation01 = BoxShadow(
    color: Color(0x52000000),
    offset: Offset(0, 1),
    blurRadius: 2,
    spreadRadius: 0,
  );

  static const darkElevation012 = BoxShadow(
    color: Color(0x1428293d),
    offset: Offset(0, 0),
    blurRadius: 1,
    spreadRadius: 0,
  );

  static const darkElevation01Inset = BoxShadow(
    color: Color(0x52000000),
    offset: Offset(0, 0.5),
    blurRadius: 4,
    spreadRadius: 0,
  );

  static const lightElevation06 = BoxShadow(
    color: Color(0x3d606170),
    offset: Offset(0, 20),
    blurRadius: 32,
    spreadRadius: 0,
  );

  static const lightElevation062 = BoxShadow(
    color: Color(0x1428293d),
    offset: Offset(0, 2),
    blurRadius: 8,
    spreadRadius: 0,
  );

  static const lightElevation05 = BoxShadow(
    color: Color(0x29606170),
    offset: Offset(0, 16),
    blurRadius: 24,
    spreadRadius: 0,
  );

  static const lightElevation052 = BoxShadow(
    color: Color(0x0a28293d),
    offset: Offset(0, 2),
    blurRadius: 8,
    spreadRadius: 0,
  );

  static const lightElevation04 = BoxShadow(
    color: Color(0x29606170),
    offset: Offset(0, 8),
    blurRadius: 16,
    spreadRadius: 0,
  );

  static const lightElevation042 = BoxShadow(
    color: Color(0x0a28293d),
    offset: Offset(0, 2),
    blurRadius: 4,
    spreadRadius: 0,
  );

  static const lightElevation03 = BoxShadow(
    color: Color(0x29606170),
    offset: Offset(0, 4),
    blurRadius: 8,
    spreadRadius: 0,
  );

  static const lightElevation032 = BoxShadow(
    color: Color(0x0a28293d),
    offset: Offset(0, 0),
    blurRadius: 2,
    spreadRadius: 0,
  );

  static const lightElevation02 = BoxShadow(
    color: Color(0x29606170),
    offset: Offset(0, 2),
    blurRadius: 4,
    spreadRadius: 0,
  );

  static const lightElevation022 = BoxShadow(
    color: Color(0x0a28293d),
    offset: Offset(0, 0),
    blurRadius: 1,
    spreadRadius: 0,
  );

  static const lightElevation01 = BoxShadow(
    color: Color(0x29606170),
    offset: Offset(0, 0.5),
    blurRadius: 2,
    spreadRadius: 0,
  );

  static const lightElevation012 = BoxShadow(
    color: Color(0x1428293d),
    offset: Offset(0, 0),
    blurRadius: 1,
    spreadRadius: 0,
  );

  static const lightElevation01Inset = BoxShadow(
    color: Color(0x52606170),
    offset: Offset(0, 0.5),
    blurRadius: 4,
    spreadRadius: 0,
  );

  AppShadows._();
}
